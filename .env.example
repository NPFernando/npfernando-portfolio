# Public-safe frontend configuration
# Copy to .env.local for local development. Do not put secrets in VITE_* values;
# Vite exposes them to the browser bundle.

VITE_GITHUB_USERNAME=NPFernando
VITE_SHOW_PROJECTS=true
VITE_SHOW_LEARNING=true
